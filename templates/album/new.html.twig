{% extends 'base.html.twig' %}

{% block title %}New Album{% endblock %}

{% block body %}
    <h1 style="text-align: center; padding-top: 2rem;">Agregar álbum</h1>
    <div class="form_new">
        {{ include('album/_form.html.twig') }}
    </div>

    <a href="{{ path('app_album_index') }}">back to list</a>
{% endblock %}
