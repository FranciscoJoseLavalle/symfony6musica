<form method="post" action="{{ path('app_album_delete', {'id': album.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete_form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    {# <button class="btn"><img src="{{ asset('img/delete.png') }}" alt="Delete"></button> #}
    <button class="btn">X</button>
</form>
